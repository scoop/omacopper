version: 2
updates:
    # npm dev tooling is updated by .github/workflows/bun-update.yml instead:
    # Dependabot's bun cannot read the lockfileVersion 2 that Bun 1.4.2 writes.
    - package-ecosystem: github-actions
      directory: /
      schedule:
          interval: weekly
